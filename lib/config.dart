import 'package:flutter/material.dart';

const Color primaryColor = Color(0xfffcfcfc);

const Color iconColor = Color(0xff130F26);

const BorderRadius defaultBorderRadius = BorderRadius.all(Radius.circular(20));
